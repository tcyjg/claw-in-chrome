# Claw 插件混淆代码恢复：任务规格计划书（可维护开发版）

## 1. 背景与目标

本仓库当前应视为“可运行发行产物 + 人工回填可维护层”的半恢复仓库，而不是可直接重建的原始源码工程。

本计划的最终目标不是 1:1 还原原作者的 TS/React/Vite 目录，而是：

- 在不破坏现有运行行为的前提下，让关键逻辑逐步变得可读、可测、可维护
- 让后续新需求优先落在可维护层，尽量减少直接改哈希 bundle 的次数

## 2. 当前现状（截至 2026-04-14）

已确认：

- 根目录缺失原始构建骨架，核心界面仍由 `manifest.json` 加载 `assets/*` 哈希 bundle 驱动。
- `DEOBFUSCATION_MAP.md` 用于记录“自动反混淆 + 人工入口标注”的进度和锚点。
- 已存在可维护层（如 service worker runtime、custom provider、github update 等），并有回归测试可跑。

本轮已完成的工作类型：

- 对关键 bundle 做“定点语义拆解”：只加注释与 `const` 语义锚点/别名，不做大规模清洗
- 添加锚点回归测试：保证锚点文本可被 `rg` 搜索且不被后续改动破坏

## 3. 不可变契约（第一优先级）

第一阶段必须冻结并保护这些隐式契约，避免破坏 bundle 与壳层之间的约定：

- `manifest.json` 的入口文件、HTML 页面、权限声明、资源路径不变
- `chrome.storage` 关键键名不变（包括前缀、scope 结构等）
- `chrome.runtime.sendMessage` / `onMessage` 的 action/type 名不变
- sidepanel/options/pairing 等页面的脚本加载顺序不变
- 已存在的 `globalThis.__CP_*` 命名空间不改名、不反向依赖新构建体系

稳定契约统一收口在 `claw-contract.js`，新增契约只能“增量追加”，不得破坏既有字段语义。

## 4. 工作方法与约束

### 4.1 对混淆 bundle 的改动规则

允许：

- 增加中文语义注释（用于标记关键入口/分支）
- 增加 `const __cp* = <原变量/函数>` 形式的语义锚点/别名
- 在不改变语义的前提下，用 `const` 替换“硬编码字符串”引用（仅当不会影响运行）

禁止：

- 大规模重排/格式化 bundle
- 替换算法/逻辑、修改返回值、改变异步时序
- 引入需要构建的 TypeScript/打包链作为运行依赖

### 4.2 对可维护层的改动规则

优先把“可变需求”写在根目录可维护模块中，通过：

- `service-worker-loader.js` 的加载顺序
- `globalThis.__CP_*` 运行时挂载点
- storage/message 的同接口替换

来覆盖/扩展 bundle 行为，而不是直接动 bundle。

## 5. 里程碑与交付物

### M0：回归底线（持续）

- `node tests/run-all-tests.js` 单次执行 < 60s
- 每次改动后必须保持通过

### M1：锚点与映射（持续迭代）

交付物：

- `DEOBFUSCATION_MAP.md`：新增锚点必须同步登记
- `tests/unit/deobfuscation-anchors.regression.test.js`：锚点存在性回归

验收：

- 关键入口（初始化、权限、会话、更新、桥接）能通过锚点快速定位

### M2：契约中心化（下一阶段重点）

交付物：

- 扩展 `claw-contract.js`：收口高频 message type、storage key、页面 query key、DOM id 等
- 扩展 `tests/unit/recovery-contract.freeze.test.js`：冻结/兼容性回归

验收：

- 新增可维护模块默认只引用 contract 常量，不再散落硬编码键名

### M3：高价值逻辑外提（按需推进）

优先候选（按收益排序）：

- detached window / session scope / 清理策略（已部分外提）
- service worker 的定时任务编排与补偿重排
- MCP bridge/permission_required 重试与提示链路
- offscreen 播放声音与 GIF 生成的消息协议与错误分支

验收：

- 外提模块具备单元/集成测试
- bundle 改动最小化，且锚点回归覆盖

## 6. 任务拆解（用于子代理并行推进）

本仓库适合“多文件独立处理”的并行模式。每个子任务必须有明确写集，避免写冲突。

推荐的工单模板：

- 目标：一句话说明要恢复/外提的能力
- 文件边界：只允许修改哪些文件
- 允许操作：只加注释/锚点/别名，或允许外提模块
- 输出：必须给出可 `rg` 搜索的锚点文本清单 + 变更文件列表
- 验收：新增/更新哪些测试

## 7. 测试与验收

### 7.1 自动化

- 基线：`node tests/run-all-tests.js`
- 锚点回归：覆盖关键 bundle 与关键可维护脚本
- 契约冻结：`recovery-contract.freeze.test.js`

### 7.2 手工冒烟（每次发版前）

- 扩展可加载（无报错）
- sidepanel 可打开并可发起一次对话
- options 保存并生效（custom provider 可切换）
- pairing 流程可触发并能确认连接
- 更新检查可触发并提示
- 独立窗口模式可打开且会话不回退

## 8. 风险与回滚策略

主要风险：

- bundle 最小改动仍可能影响运行（拼写错误、插入位置破坏作用域）
- 键名/消息名漂移导致“静默失效”（最难排查）

回滚策略：

- 所有对 bundle 的改动都必须伴随锚点回归测试
- 如出现行为异常，优先回滚最近一次 bundle 变更，再用更小粒度锚点重做

## 9. 下一轮并行推进建议（默认）

建议下一轮优先并行推进这三类工作：

1. 扩展 `claw-contract.js` 的 message/storage 契约面（不破坏旧字段）
2. 为 offscreen/agent indicator/pairing 等“跨页面协议”补充契约与回归
3. 选择 1 条高价值链路做外提试点（例如 MCP permission_required 重试链）

## 10. 下一阶段解混淆执行计划（自 2026-04-15 起）

本阶段的重点从“协议补洞”切换到“业务纵切面恢复”。

目标不是继续零散补锚点，而是把高价值主链恢复成：

- 可按业务理解
- 可按链路定位
- 可按文档维护
- 可被测试保护

### 10.1 总体原则

1. 优先解高价值主链，不横扫全文件
2. 优先恢复语义和链路，再决定是否外提 helper
3. 对 bundle 仍坚持最小改动：只加语义别名、中文链路注释、最小常量收口
4. 每轮都必须同步：
   - `DEOBFUSCATION_MAP.md`
   - `docs/message-protocol.md`
   - 对应回归测试

### 10.2 三轮推进顺序

#### 第一轮：权限 / 配对主链彻底解混淆

目标：

- 还原 `permission_required -> sidepanel/UI -> runtime 回包 -> background pending promise`
- 还原 sidepanel 内联 pairing 与独立 pairing 页的职责边界
- 彻底区分 `requestId` / `tabId` / `toolUseId` 的角色

重点文件：

- `assets/sidepanel-BoLm9pmH.js`
- `assets/mcpPermissions-qqAoJjJ8.js`
- `assets/pairing-H3Cs7KHl.js`
- `assets/PairingPrompt-Do4C6yFu.js`

交付物：

- 新一批权限/配对主链语义别名
- 权限/配对链路文档更新
- 锚点回归同步

#### 第二轮：会话恢复 / detached window 主链解混淆

目标：

- 还原 scope hydrate、snapshot/draft 持久化、detached window 恢复与锁同步

重点文件：

- `assets/sidepanel-BoLm9pmH.js`
- `service-worker-detached-window-runtime.js`
- `service-worker-runtime.js`
- `assets/service-worker.ts-H0DVM1LS.js`

交付物：

- 会话恢复链路图
- detached window 生命周期文档
- 必要的 contract/helper 外提候选清单

当前进度（2026-04-15）：

- 已完成 `service-worker-detached-window-runtime.js`
  - detached lock 账本归一化/读写/巡检链已补清
  - detached window URL 协议（`mode/tabId/groupId`）已固定
  - “复用现有 popup / 新建 popup / 回写 lock”主链已补中文锚点
- 已完成 `service-worker-runtime.js`
  - scope cleanup runtime 的索引层、closed-group 清理链、orphan-scan 清理链已补清
  - `OPEN_GROUP_DETACHED_WINDOW` 的 runtime handler 职责边界已单独标明
  - host window / main tab 销毁后的 detached popup 收口链已补清
- 已同步第二轮文档/回归
  - `docs/session-recovery-detached-window.md` 已建立
  - runtime 层锚点已纳入 `tests/unit/deobfuscation-anchors.regression.test.js`
- 本轮剩余重点
  - 继续把 `assets/sidepanel-BoLm9pmH.js` 与 `assets/service-worker.ts-H0DVM1LS.js` 的恢复/协调链对齐
  - 补完第二轮文档与锚点回归

#### 第三轮：MCP tool executor / service worker 协调链解混淆

目标：

- 还原 `tool_call -> tool executor -> tab 编排 -> indicator -> tool_result`

重点文件：

- `assets/mcpPermissions-qqAoJjJ8.js`
- `assets/service-worker.ts-H0DVM1LS.js`
- `assets/agent-visual-indicator.js-Ct7LqXhp.js`
- `assets/accessibility-tree.js-D8KNCIWO.js`

交付物：

- tool executor 语义别名集
- runtime/message 类型索引补全
- 对应回归测试

### 10.3 第一轮的并行拆解方案

本轮默认采用并行推进：

1. sidepanel / pairing 子链
   - 写集：
     - `assets/sidepanel-BoLm9pmH.js`
     - `assets/pairing-H3Cs7KHl.js`
     - `assets/PairingPrompt-Do4C6yFu.js`

2. background / MCP permission promise 子链
   - 写集：
     - `assets/mcpPermissions-qqAoJjJ8.js`

3. 主线程集成与验收
   - 写集：
     - `DEOBFUSCATION_MAP.md`
     - `docs/message-protocol.md`
     - `tests/unit/deobfuscation-anchors.regression.test.js`
     - 必要的单测 / freeze 测试

### 10.4 每轮完成标准

每一轮解混淆必须满足：

- 能说清完整业务流
- 关键符号具备稳定语义名
- 中文链路注释已落地
- 文档与测试已同步
- `node tests/run-all-tests.js` 通过
- 不改变现有运行行为
